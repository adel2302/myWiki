<%= simple_form_for @article do |f| %>
  <p><%= f.input :title %></p>
  <p><%= f.input :content %></p>
  <p><%= f.collection_select :category_id, Category.all, :id, :name, { prompt: "Choisir une catégorie" } %></p>
  <p><%= f.submit %></p>
<% end %>